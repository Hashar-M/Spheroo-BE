package com.qburst.spherooadmin.supplier;

/**
 * enums specified is used in searching a {@link Supplier}.
 * By using enum we could specify the only acceptable search parameters mentioned in {@link com.qburst.spherooadmin.search.SupplierPaginationFilter}.
 */
public enum SupplierPagingConstraint {
    supplierName,supplierId,name
}
